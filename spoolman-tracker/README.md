# Home Assistant Add-on: Spoolman Tracker

Wrapper add-on for [`ghcr.io/nozzle-1/spoolman-tracker:0.1.0`](https://ghcr.io/nozzle-1/spoolman-tracker).

The add-on converts Home Assistant UI options into the `config.json` expected by the application, then starts the tracker container process.
